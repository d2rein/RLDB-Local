Import and data-prep scripts will live here.

Planned first scripts:

- import reconciled matches
- import venues and team aliases
- import AFLTables legacy scorer data
- import NRL detailed match stats
- import NRL player statistics
- emit unresolved alias/matching issues
